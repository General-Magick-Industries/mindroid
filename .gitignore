# Build artifacts
/target

# Environment and secrets
.env*
!.env.example
# Example configs carry real provider API keys — keep them out of git.
examples/artifact_agent/config.toml
# Local artifact store + sqlite db produced by running the example.
artifact-agent-store/

# IDE / editor
.zed/
.idea/
.vscode/
*.swp
*.swo
*~

# AI tooling
.claude/
.omc/
# AGENTS.md is intentionally committed — it is the shared, cross-tool source of
# truth for agent + contributor guidance (see docs/adr/). CLAUDE.md imports it.

# OS
.DS_Store
Thumbs.db

# Rust-specific
*.pdb
**/*.rs.bk
rust-toolchain.toml

# CI artifacts
clippy-output.txt
audit-output.txt

# Database files (from sqlite memory)
*.db
*.db-journal
*.db-wal

# Local run configs holding real credentials — never commit
*.local.toml

# Local review notes — not for publication
pr26-*.md
