# Build artifacts
/target

# Environment and secrets
.env*
!.env.example

# IDE / editor
.zed/
.idea/
.vscode/
*.swp
*.swo
*~

# AI tooling
.claude/
.omc/
# AGENTS.md is intentionally committed — it is the shared, cross-tool source of
# truth for agent + contributor guidance (see docs/adr/). CLAUDE.md imports it.

# OS
.DS_Store
Thumbs.db

# Rust-specific
*.pdb
**/*.rs.bk
rust-toolchain.toml

# CI artifacts
clippy-output.txt
audit-output.txt

# Database files (from sqlite memory)
*.db
*.db-journal
*.db-wal

# Local run configs holding real credentials — never commit
*.local.toml

# Kubeconfigs (cluster bearer tokens) — never commit
*kubeconfig*
*.kubeconfig
nuos-eks-dev*.yaml

# Local review notes — not for publication
pr26-*.md
